<UserControl x:Class="InputSoundPlugin.SettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="500">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <DockPanel Margin="0,5">
                <TextBlock Text="機能の有効化" VerticalAlignment="Center" FontWeight="Bold"/>
                <CheckBox IsChecked="{Binding IsEnabled}" HorizontalAlignment="Right"/>
            </DockPanel>

            <Border BorderThickness="0,0,0,1" BorderBrush="#33000000" Margin="0,5"/>

            <Grid Margin="0,2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="音量" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1" Text="{Binding Volume, StringFormat={}{0}%}" VerticalAlignment="Center" TextAlignment="Right" Margin="0,0,10,0" Opacity="0.6"/>
                <Slider Grid.Column="2" Minimum="0" Maximum="100" Value="{Binding Volume}" VerticalAlignment="Center"/>
            </Grid>

            <Grid Margin="0,2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="最大再生時間" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1" Text="{Binding MaxDuration, StringFormat={}{0:F1}s}" VerticalAlignment="Center" TextAlignment="Right" Margin="0,0,10,0" Opacity="0.6"/>
                <Slider Grid.Column="2" Minimum="0" Maximum="10" TickFrequency="0.1" IsSnapToTickEnabled="True" Value="{Binding MaxDuration}" VerticalAlignment="Center"/>
            </Grid>

            <Grid Margin="0,2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="同時再生制限" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1" Text="{Binding MaxConcurrentSounds}" VerticalAlignment="Center" TextAlignment="Right" Margin="0,0,10,0" Opacity="0.6"/>
                <Slider Grid.Column="2" Minimum="0" Maximum="20" Value="{Binding MaxConcurrentSounds}" VerticalAlignment="Center"/>
            </Grid>
        </StackPanel>

        <GroupBox Header="ツール" Grid.Row="1" Margin="0,0,0,10" Padding="8">
            <StackPanel>
                <Button Content="すべてのキー/マウスにフォルダで一括設定" Click="ApplyFolderToAll_Click" Margin="0,0,0,8" Height="28"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="検索：" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBox Grid.Column="1" x:Name="SearchBox" TextChanged="SearchBox_TextChanged" VerticalContentAlignment="Center" Height="24"/>
                    <Button Grid.Column="2" x:Name="WaitButton" Content="キー入力で検索" Click="WaitButton_Click" Margin="8,0,0,0" Padding="12,0"/>
                    <Button Grid.Column="3" Content="リセット" Click="Reset_Click" Margin="8,0,0,0" Padding="12,0" Height="24"/>
                </Grid>
            </StackPanel>
        </GroupBox>

        <TabControl Grid.Row="2" Background="Transparent">
            <TabItem Header="キーボード">
                <ListBox ItemsSource="{Binding KeySettings}" HorizontalContentAlignment="Stretch" BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border BorderThickness="0,0,0,1" BorderBrush="#11000000" Padding="0,5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="110"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="35"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" VerticalAlignment="Center"/>
                                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                        <CheckBox Content="押" IsChecked="{Binding OnPress}" Margin="8,0"/>
                                        <CheckBox Content="離" IsChecked="{Binding OnRelease}" Margin="8,0"/>
                                    </StackPanel>
                                    <ItemsControl Grid.Column="2" ItemsSource="{Binding Paths}" Margin="10,0">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <DockPanel Height="20">
                                                    <Button Content="×" Click="RemovePath_Click" DockPanel.Dock="Right" Width="16" Height="16" FontSize="10" VerticalAlignment="Center" Background="Transparent" BorderThickness="0"/>
                                                    <TextBlock Text="{Binding}" FontSize="10" Opacity="0.6" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                                </DockPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <Button Grid.Column="3" Content="+" Click="AddPath_Click" Width="24" Height="24" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </TabItem>
            <TabItem Header="マウス">
                <ListBox ItemsSource="{Binding MouseSettings}" HorizontalContentAlignment="Stretch" BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border BorderThickness="0,0,0,1" BorderBrush="#11000000" Padding="0,5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="110"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="35"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" VerticalAlignment="Center"/>
                                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                        <CheckBox Content="押" IsChecked="{Binding OnPress}" Margin="8,0"/>
                                        <CheckBox Content="離" IsChecked="{Binding OnRelease}" Margin="8,0"/>
                                    </StackPanel>
                                    <ItemsControl Grid.Column="2" ItemsSource="{Binding Paths}" Margin="10,0">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <DockPanel Height="20">
                                                    <Button Content="×" Click="RemovePath_Click" DockPanel.Dock="Right" Width="16" Height="16" FontSize="10" VerticalAlignment="Center" Background="Transparent" BorderThickness="0"/>
                                                    <TextBlock Text="{Binding}" FontSize="10" Opacity="0.6" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                                </DockPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <Button Grid.Column="3" Content="+" Click="AddPath_Click" Width="24" Height="24" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>